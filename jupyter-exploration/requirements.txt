# Submission notebook runtime — install into a fresh Python ≥3.10 venv:
#   pip install -r requirements.txt
# Lower bounds match `pyproject.toml`; tested against pandas 3.0 / numpy 2.4 /
# scikit-learn 1.8 / matplotlib 3.10.
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
matplotlib>=3.8
jupyter>=1.1
